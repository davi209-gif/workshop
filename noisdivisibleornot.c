#include <stdio.h>
int main()
{
printf("Checking the number is divisible by 2 or not");

int x;

printf("\n Input any number : " );

scanf("%d",&x);

printf("\n%d",x%2==0);

printf("\nIf the output is 1 means divisible by 2 and If the output is not 1 it means not divisble by 2");








    return 0;
}